{
  "name": "@game-ci/rust",
  "version": "0.1.0",
  "private": true,
  "description": "EXPERIMENTAL: Rust engine plugin. Builds any Rust game (Bevy, macroquad, ggez, Fyrox, or plain wgpu/winit) via cargo - engine-agnostic within Rust, since they all build the same way.",
  "license": "MIT",
  "repository": "git@github.com:game-ci/cli.git",
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "bun": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^17.0.23",
    "typescript": "4.7.4",
    "vitest": "^4"
  },
  "engines": {
    "node": ">=18.x"
  }
}
